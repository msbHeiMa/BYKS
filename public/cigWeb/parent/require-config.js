/* require config中的项暂时要用JSON格式，fis时暂时不支持写function */
requirejs.config({
    paths: {
        systemConfig:'/cigWeb/parent/config',
        menuTree:'/cigWeb/parent/menutree',
        breadcrumb:'/cigWeb/parent/breadcrumb',
    }
});