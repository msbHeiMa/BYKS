#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.3.4.32@fis3/bin/fis.js" "$@"
  ret=$?
else 
  node  "$basedir/../.3.4.32@fis3/bin/fis.js" "$@"
  ret=$?
fi
exit $ret
